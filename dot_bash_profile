#.bash_profile

# Get the aliases and functions
[ -f $HOME/.bashrc ] && . $HOME/.bashrc

export BROWSER="brave-browser"
export TERMINAL="kitty"
export LOCATION="Loveland"
export EDITOR="nvim"
export FILE_MANAGER="yazi"
export XDG_SCREENSHOT_DIR="$HOME/Screenshots"

# Run OnlyOffice under XWayland
# env GDK_BACKEND=x11 ./DesktopEditors.AppImage

if [ -z "$WAYLAND_DISPLAY" ] && [ "$(tty)" = "/dev/tty1" ]; then
    exec dbus-run-session -- sway
fi

# if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
#     echo "Select your window manager:"
#     echo "1) Hyprland"
#     echo "2) i3"
#     echo "3) Sway"
#     read -p "Enter choice [1, 2, or 3]: " wm_choice

#     case "$wm_choice" in
#         1)
#             exec Hyprland
#             ;;
#         2)
#             exec startx 
#             ;;
#         3)
#             exec sway 
#             ;;
#         *)
#             echo "Invalid choice. Exiting."
#             ;;
#     esac
# fi
# [[ $(ps -e | grep hyprland) = '' ]] && hyprland 

. "$HOME/.cargo/env"
. "$HOME/.atuin/bin/env"
